component
  Query
    input
      tick();
      read():R{0,100};
    
  implementation hfsm( A )
  
    transition
      A to B by tick();
      B to C by tick();
      C to A by tick();
  
    state A
    end
      
    state B
      function read():R{0,100}
        return 42;
      end
    end

    state C
    end
    
    function read():R{0,100}
      return 0;
    end
  
  end

