component
  Query3
    input
      tick();
      read():R{0,100};
    
  implementation hfsm( A )
  
    transition
      A to B by tick();
      B to C by tick();
      C to A by tick();
  
    function read():R{0,100}
      return 57;
    end
  
    state A
    end
      
    state B
      function read():R{0,100}     // this query is never used
        return 42;
      end
    end

    state C
    end
    
  end

