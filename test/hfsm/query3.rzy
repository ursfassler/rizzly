component
  Query3
    slot     tick();
    response read():R{0,100};
    
  implementation hfsm( A )
  
    transition
      A to B by tick();
      B to C by tick();
      C to A by tick();
  
    response read():R{0,100}
      return 57;
    end
  
    state A
    end
      
    state B
      response read():R{0,100}     // this response is never used
        return 42;
      end
    end

    state C
    end
    
  end

