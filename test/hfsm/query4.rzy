Query4 = Component
  tick : slot();
  read : response( x: R{0,10} ):R{0,100};
    
hfsm( A )
  A to B by tick();
  B to C by tick();
  C to A by tick();

  A : state
    read : response( x: R{0,10} ):R{0,100}
      return x * 1;
    end
  end
    
  B : state
    read : response( x: R{0,10} ):R{0,100}
      return x * 2;
    end
  end

  C : state
    read : response( x: R{0,10} ):R{0,100}
      return x * 3;
    end
  end
  
end

