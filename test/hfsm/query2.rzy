component
  Query2
    slot     tick();
    response read():R{0,100};
    
  implementation hfsm( A )
  
    transition
      A to B by tick();
      B to C by tick();
      C to A by tick();
  
    state A
    end
      
    state B
    end

    state C
    end
    
    response read():R{0,100}
      return 0;
    end
  
  end

