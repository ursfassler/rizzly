StateVariable2 = Component
  tick : slot();

hfsm(A)
  A : state
    a : R{0,255} = 0;
      
    tuwas = procedure()
      a := (a + 2) and 255;
    end
    
    A to A by tick() do
      a := (a + 1) mod 256;
    end
  end
end

