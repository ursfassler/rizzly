//test ok

StateNames1 = Component
  tick : slot();
  evt : signal( value: R{0,255} );

hfsm(A)
  A : state(A)
    A : state(A)
      A : state;
      B : state;
      C : state;
    end
    B : state(A)
      A : state;
      B : state;
      C : state;
    end
  end
end

