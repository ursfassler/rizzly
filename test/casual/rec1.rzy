Point = Record
  x : R{0,100};
  y : R{0,100};
end

Rec1 = Component
  out : signal( x: R{0,100} );
  ind : slot( x: R{0,100} );

elementary
  ind : slot( x: R{0,100} )
    p : Point;
    p.x := x;
    p.y := 100 - p.x;
    out( p.y );
  end

end

