Locvar = Component
  ind : response( x: R{0,255} ):R{0,255};

elementary
  ind : response( x: R{0,255} ):R{0,255}
    if x = 0 then
      return 0;
    else
      t : R{0,510} = x * 2;
      if t > 255 then
        t := 255;
      end
      return R{0,255}( t );
    end
    return 0;
  end

end

