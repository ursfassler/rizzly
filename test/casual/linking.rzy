interface
  Foo
    function bar();
  end

component
  Linking
    input
      in  : Foo;

    output
      linking   : Foo;    // it has the same name as namespace ("linking"), may cause problem; should not
    
  implementation elementary
  
    function in.bar
      linking.bar;
    end
  
  end

