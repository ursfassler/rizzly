component
  Linking
    slot   ind();
    signal linking();    // it has the same name as namespace ("linking"), may cause problem; should not
    
  implementation elementary
  
    slot ind()
      linking();
    end
  
  end

