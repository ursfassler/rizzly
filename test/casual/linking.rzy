component
  Linking
    input
      in();

    output
      linking();    // it has the same name as namespace ("linking"), may cause problem; should not
    
  implementation elementary
  
    function in()
      linking();
    end
  
  end

