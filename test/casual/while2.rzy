While2 = Component
  out : signal( x: R{0,100} );
  ind : slot( x: R{0,100} );

elementary
  ind : slot( x: R{0,100} )
    while x > 0 do
      out( x );
      x := R{0,100}( x-1 );
    end
  end
end

