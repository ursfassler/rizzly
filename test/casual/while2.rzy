component
  While2

    input
      ind( x: R{0,100} );

    output
      out( x: R{0,100} );

    implementation elementary

    function ind( x: R{0,100} )
      while x > 0 do
        out( x );
        x := R{0,100}( x-1 );
      end
    end
  end

