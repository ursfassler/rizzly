

function something( x: R{0,255} ):R{0,255}
  return (x + 2) mod 256;
end

const
  Answer : R{0,255} = something( 42 );

component
  ConstInit

    implementation elementary

    const
      BlaBla : R{0,255} = something( 23 );
      BluBlu : R{0,255} = Answer;
      BleBle : R{0,Answer} = 1;
      BloBlo : R{0,255} = (Answer + 10) / 3;

    var
      a : R{0,Answer};
      b : R{0,Answer+2};
      
    component
      comp1 : Dummy{1};
      comp2 : Dummy{Answer/2};

    function foo()
      bar : R{0,255} = Answer;
      bar := something( bar );
    end

  end

  Dummy{S:R{0,255}}
  
  implementation elementary
  
  const
    Dodo : R{0,S} = 1;
    Dudu : R{0,255} = S;
    Dada : R{-100,100} = something(S) / 2 - 100;
  
  var
    a	 : R{0,S};
    b : R{0,(S+10)/2};
  
  end


