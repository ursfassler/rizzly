data : const Array{10,R{0,100}} = (
    10,
    20,
    1,
    2,
    3,
    4,
    100,
    5,
    12,
    7 
  );

ArrayValue = Component
  out : signal( x: R{0,100} );
  inp : slot( x: R{0,100} );

elementary
  inp : slot( x: R{0,100} )
    out( data[x] );
  end

end

