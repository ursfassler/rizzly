component
  Arr
    input
      in( x: Array{10,R{0,100}} );
    
    output
      out( x: Array{3,R{0,100}} );

    implementation elementary
    
    var
      a : Array{10,R{0,100}};
    
    function in( x: Array{10,R{0,100}} )
      a := x;
      b : Array{3,R{0,100}};
      b[0] := a[0];
      b[1] := a[2];
      b[2] := a[5];
      out( b );
    end

  end

